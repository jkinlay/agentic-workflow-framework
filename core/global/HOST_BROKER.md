# Broker boundary

The example broker is disabled and is not a supplied native dispatcher. See the installed [specification](../.agentic/SPECIFICATION.md). Enabling an external broker needs its own authenticated permission, identity and resource enforcement.
