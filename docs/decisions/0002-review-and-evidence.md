# Review and evidence restoration

The re-review identified over-removal in the first consolidation. Keep one Apache-2.0 core and preserve history, but retain the reusable split external-review adapter inside that core. This supersedes the previous decision's statement that no Claude adapter ships; it does not restore duplicate scaffold trees or change the accepted required engine.

The root external gate and project policy remain unchanged. The restored Claude workflow is explicit opt-in, uses protected base code and separates model and publisher credentials. Native review, scheduled Codex amendments and external-engine qualification remain distinct. No qualification flag is inferred from offline tests or historical demonstrations.

Documentation uses substantive short prompts, migration procedures and five focused runbooks. Per-file limits replace the global cap; reported totals include generated prompt copies and separately identify the generated manifest. The native benchmark measures structured decisions and citations, not authenticated host execution or comparative model quality. Preserve failed evaluations and rubric corrections rather than silently editing observations.

Reconciliation records operator assertions, preserves charges/cohorts and bounds repeated incident retries at ticket admission. Safe closure remains available after retry exhaustion. Signature key creation is deferred pending custody approval; unsigned status and the publisher-designated private security contact are explicit.

This candidate remains a draft pending current-head external review, live qualification and human merge acceptance. Existing project baselines remain unchanged by a user-level skill upgrade.
