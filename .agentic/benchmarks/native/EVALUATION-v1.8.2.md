# AWF v1.8.2 historical evaluation summary

This is a sanitized aggregate of an earlier three-case synthetic decision
evaluation. Its strict result was **1/3 FAIL**. The result is retained only to
avoid relabelling a measured failure as a current-release success.

The historical exercise evaluated structured decisions against a fixed rubric.
It did not execute project actions and did not qualify a provider, host,
credential, model identity, or external-review integration. It is neither a
model-quality comparison nor evidence of current prompt behavior.

No prompts, responses, traces, timestamps, account identities, host details,
usage, packets, hashes, raw logs, or external evidence locations are included
in this upstream distribution. Any future evaluation requires a separately
reviewed protocol, fresh authority, and independently retained evidence.
