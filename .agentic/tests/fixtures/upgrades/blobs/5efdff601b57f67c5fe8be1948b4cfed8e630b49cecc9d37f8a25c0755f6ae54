# Owner authorization — UNFILLED

Never post this template as approval. Render only a fresh validated request's exact `expected_text` for the current candidate/gate, using `project_status.py gate-handoff`. State the configured source channel and action. Authenticate human identity/quorum and reject edited, expired or replayed records. See [specification](../SPECIFICATION.md).
